using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RecetteFini : MonoBehaviour
{
    // Start is called before the first frame update
    public bool FinRecette;
    void Start()
    {
        FinRecette = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
